# 现象合同智能管理平台前端开发规范

> 本文件适用于“现象合同智能管理平台”前端项目中的代码开发、问题修复、重构和文档维护。

---

## 开发前必读

开始正式开发前，应先阅读 [`description/project.md`](description/project.md)，再通过 [`description/readme.md`](description/readme.md) 查找与当前任务相关的说明。

开发前必须检查现有代码、配置和文档，以可观察事实为依据理解当前行为。需求、实现边界或外部契约尚不明确时，不得把猜测写成既定规则；优先继续调查，确实无法确认时再向需求方提出具体问题。

---

## 开发原则

1. **正确性优先**：先明确输入、输出、状态变化、异常路径和验收条件，再开始实现。

2. **职责清晰**：模块、组件和函数应保持单一职责，依赖方向明确，对外接口稳定，避免跨层访问内部细节。

3. **抽象适度**：只为已经存在的重复、变化或复杂度建立抽象，不为套用模式提前设计不存在的扩展点。

4. **改动聚焦**：实现满足需求所需的最小完整改动，不顺带重写无关代码，不扩大任务影响范围。

5. **保持一致**：新增实现应优先遵循项目现有的命名、目录、代码风格和交互模式；确需改变约定时，应说明原因并同步更新相关文档。

6. **边界显式**：业务规则、外部接口、配置、环境差异和失败策略应在合适的代码或文档层级明确表达，避免依赖隐含约定。

7. **安全默认**：不在代码或文档中提交密钥、令牌和真实敏感数据；处理用户输入、外部响应和持久化数据时应考虑校验与失败边界。

---

## 代码与注释

- 使用清晰、稳定且能表达业务含义的命名，避免依赖上下文才能理解的缩写。
- 函数和组件保持短小聚焦；复杂流程优先拆分为可独立理解、验证和复用的单元。
- 只在业务规则、边界条件、兼容原因和关键设计取舍处添加中文注释，不为显而易见的语句重复注释。
- 不保留失效代码、无效引用、调试输出或没有明确用途的占位实现。
- 引入依赖前先确认现有能力无法合理满足需求，并评估体积、维护状态、安全性和替换成本。

---

## 验证要求

完成改动后，应根据风险执行与改动相称的验证：

1. 先验证直接受影响的功能和主要失败路径。
2. 再执行项目已有的构建、测试、静态检查或格式检查命令。
3. 涉及配置、路由、资源路径或外部接口时，应验证真实运行链路，不能只依赖编译成功。
4. 无法执行某项验证时，应明确说明未验证内容、原因和潜在风险。

修复缺陷时，应确认根因已经消除，并检查相邻路径是否存在同类问题；避免只针对表面报错增加特殊判断。

---

## 文档要求

行为、架构、接口、配置或运行方式发生实质变化时，应同步更新 `description/` 下对应文档。纯格式调整或不影响理解的局部实现变化，不要求机械新增文档。

文档应说明稳定的用途、边界、决策和使用方式，不复制容易与代码漂移的大段实现细节。文档命名、结构和链接维护遵循[文档撰写风格手册](description/documentation.md)。

---

## 协作边界

- 尊重工作区中已有但与当前任务无关的改动，不覆盖、不回退、不擅自整理。
- 删除、迁移或重命名文件前，应确认引用关系和影响范围。
- 对具有破坏性、不可逆或外部影响的操作，先明确目标并采取可恢复措施。
- 交付时说明完成内容、验证结果、仍存在的限制以及需要后续处理的事项。
